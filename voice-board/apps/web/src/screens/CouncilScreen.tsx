export function CouncilScreen() {
  return (
    <div>
      <h2 style={{ marginTop: 0 }}>Council workspace</h2>
      <div style={{ color: '#555' }}>
        MVP placeholder. The API has the foundations for AI workflows (agenda/transcript/resolution) but the UI is intentionally minimal.
      </div>
    </div>
  );
}
